{{#if done}}
<a id="clear-completed">Clear completed items</a>
{{/if}}
<div class="todo-count">
    <b>{{remaining}}</b>
    <span>{{#ifCond remaining 1}}item{{else}}items{{#ifCond}} left</span>
</div>
