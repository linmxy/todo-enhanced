<div class="view">
    <input type="checkbox" class="toggle" value="done" {{#if completed}}checked{{/if}}/>

    <label class="todo-text">{{title}}</label>
    <button class="destroy"></button>
</div>
<input class="edit" value="{{title}}">
